package com.aashoo17.imagesandgraphics

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

/*
VectorDrawable:
so its similar to svg on web, but svg is vast and does lot of thinks on web not required in android
so born the VectorDrawable
still drawing it by hand is i would say not easy
so android studio provides a way to convert any svg to VectorDrawable type
go to res/drawable -> new -> vector asset -> Local File(SVG,PSD)
 */

class VectorDrawableCustom : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }
}